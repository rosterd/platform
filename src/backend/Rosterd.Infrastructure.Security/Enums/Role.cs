namespace Rosterd.Infrastructure.Security.Enums
{
    /// <summary>
    /// The security roles available for the system
    /// </summary>
    public enum Role
    {
        /// <summary>
        /// Admin role for a given tenant, able to perform all tasks for that tenant
        /// </summary>
        TenantAdmin = 1
    }
}
